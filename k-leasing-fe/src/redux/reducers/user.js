import update from "immutability-helper";
import * as types from "../types";

const initalState = {
    
};
const user = (state = initalState, action = {}) => {
    switch (action.type) {

        

        default:
            return state;
    }
};
export default user;
